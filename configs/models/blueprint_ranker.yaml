schema_version: "1.0"
ranker_id: knn_blueprint_ranker_v1

relevance:
  answer_flow_weight: 0.55
  retrieval_action_weight: 0.45
  exact_match_grade: 3
  strong_match_threshold: 0.75
  partial_match_threshold: 0.40

prediction:
  neighbor_relevance_weight: 0.65
  semantic_compatibility_weight: 0.35
  k_values:
    - 1
    - 3
    - 5

evaluation:
  ndcg_k:
    - 3
    - 5
  recall_k:
    - 1
    - 3
    - 5
  split_strategy: LEAVE_ONE_PARENT_GROUP_OUT
